apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: coder
    repo: oci://ghcr.io/coder/chart
    version: 2.33.2
    releaseName: coder
    namespace: coder
    valuesFile: values.yaml

resources:
  - postgres-external-secret.yaml
  - postgres.yaml
  - ingress.yaml
