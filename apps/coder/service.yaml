apiVersion: v1
kind: Service
metadata:
  name: coder
  namespace: coder
spec:
  # chart 既定は LoadBalancer だが、外部公開は Tailscale Ingress が担うため ClusterIP。
  type: ClusterIP
  selector:
    app: coder
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
