apiVersion: v1
kind: Service
metadata:
  name: ente-museum
  namespace: ente
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: ente-museum
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
