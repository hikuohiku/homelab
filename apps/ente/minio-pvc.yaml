apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ente-minio-data
  namespace: ente
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 50Gi
