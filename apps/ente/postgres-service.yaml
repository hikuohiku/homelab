apiVersion: v1
kind: Service
metadata:
  name: ente-postgres
  namespace: ente
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  selector:
    app: ente-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
