apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NATS JetStream — cluster 内のイベント経路。
# namespace は既存の autopilot を使うため namespace.yaml は無い
resources:
  - pvc.yaml
  - config.yaml
  - external-secret.yaml
  - deployment.yaml
  - service.yaml
