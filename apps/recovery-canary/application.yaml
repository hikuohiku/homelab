apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: recovery-canary
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/hikuohiku/homelab.git
    targetRevision: HEAD
    path: apps/recovery-canary

  destination:
    server: https://kubernetes.default.svc
    namespace: recovery-canary

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
