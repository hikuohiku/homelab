apiVersion: v1
kind: Service
metadata:
  name: ops-dashboard
  namespace: autopilot
  labels:
    app: ops-dashboard
spec:
  selector:
    app: ops-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
