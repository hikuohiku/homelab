apiVersion: v1
kind: Service
metadata:
  name: ops-dashboard
  namespace: ops-dashboard
spec:
  # 外部公開は Tailscale Ingress が担うため ClusterIP (apps/coder と同じパターン)
  type: ClusterIP
  selector:
    app: ops-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
