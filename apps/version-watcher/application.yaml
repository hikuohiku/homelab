apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: version-watcher
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/hikuohiku/homelab.git
    targetRevision: HEAD
    path: apps/version-watcher

  destination:
    server: https://kubernetes.default.svc
    namespace: version-watcher

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
