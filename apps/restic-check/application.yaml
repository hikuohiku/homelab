apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: restic-check
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/hikuohiku/homelab.git
    targetRevision: HEAD
    path: apps/restic-check

  destination:
    server: https://kubernetes.default.svc
    namespace: restic-check

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
